{
  "name": "@b3b/cherry-monorepo",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/cherry",
      "examples/*"
    ],
    "catalogs": {
      "prod-npm-preview": {
        "@b3-business/cherry": "0.2.4"
      }
    }
  },
  "scripts": {
    "check": "bun run --filter '*' check",
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
