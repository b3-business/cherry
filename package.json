{
  "name": "@b3b/cherry-monorepo",
  "private": true,
  "workspaces": [
    "packages/cherry",
    "examples/*"
  ],
  "scripts": {
    "check": "bun run --filter '*' check",
    "test": "bun run --filter '*' test",
    "test:prod": "bun run scripts/test-examples-prod.bun.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "oxlint": "^1.36.0",
    "oxlint-tsgolint": "latest",
    "typescript": "^5.9.3"
  }
}
