{
  "name": "@b3b/cherry-example-pokeapi",
  "version": "0.1.0",
  "private": true,
  "description": "Cherry example using PokeAPI",
  "type": "module",
  "scripts": {
    "check": "oxlint --type-aware --type-check src test",
    "format": "oxfmt --write src test",
    "format:check": "oxfmt --check src test",
    "typecheck": "tsgo --noEmit -p .",
    "typecheck:tsc": "tsc --noEmit -p .",
    "test": "bun test"
  },
  "dependencies": {
    "@b3-business/cherry": "workspace:*",
    "neverthrow": "^8.2.0",
    "valibot": "^1.2.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.5",
    "typescript": "^5.9.3"
  }
}
